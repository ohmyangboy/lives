# 官网协作约定

遵循上级 [AGENTS.md](../AGENTS.md)。页面源码与资源位于 `src/`，沿用相邻文件风格与既有文案入口。

- 页面代码改动运行 `npm run website:build`；布局或交互变化检查实际页面，报告观察到的结果。
- 纯文档、文案改动核对内容、链接与相关命令即可，不默认运行应用测试或构建。
- 构建不等于部署成功。上传、Pages / 服务器部署及发行变更按授权范围读取共享发布与 Actions 规则，不把本地修改授权扩大为发布授权。
